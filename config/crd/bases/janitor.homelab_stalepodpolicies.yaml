---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: stalepodpolicies.janitor.homelab
spec:
  group: janitor.homelab
  names:
    kind: StalePodPolicy
    listKind: StalePodPolicyList
    plural: stalepodpolicies
    singular: stalepodpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: desired state of StalePodPolicy.
            properties:
              ttl:
                description: TTL is how long a pod may remain in a terminal phase
                  before it is deleted.
                type: string
            required:
            - ttl
            type: object
          status:
            description: StalePodPolicyStatus defines the observed state of StalePodPolicy.
            properties:
              deletedCount:
                description: DeletedCount is the number of pods deleted under this
                  policy so far.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
